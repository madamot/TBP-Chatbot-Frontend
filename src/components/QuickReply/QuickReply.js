import React from 'react';
import styled, { css } from 'styled-components';
import { action } from '@storybook/addon-actions';


export default function QuickReply({ buttons, platform }) {

    return (
      <div className="list-items">
        <p>hi</p>
      </div>
    );
}
